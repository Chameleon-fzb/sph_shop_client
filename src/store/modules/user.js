import { getUserTempId } from '@/utils/userAbout'

const state = {
	//获取临时标识id
	userTempId: getUserTempId()
}
const mutations = {}
const actions = {}
const getters = {}

export default {
	state,
	mutations,
	actions,
	getters
}
